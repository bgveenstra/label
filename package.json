{
  "name": "github-org-labels",
  "version": "1.0.0",
  "description": "set up labels for repos in a github organization",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.80.0"
  }
}
